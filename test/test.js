var fs = require('fs'),
    should = require('should'),
    async = require('async');

describe('Creating lists', function () {
  //Create a stream of arrivals

  //Create a stream of departures

  //Now, they should be able to emit connections
  it('should be emitting connections', function () {

  });

})
